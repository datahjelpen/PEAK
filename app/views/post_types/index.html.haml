%h1 Listing posts

%table
  %tr
    %th Id
    %th Title
    %th Text
  - @posts.each do |post|
    %tr
      %td= post.id
      %td= post.title
      %td= post.text
      %td= link_to 'Show', post_path(post)